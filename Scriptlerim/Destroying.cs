using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Destroying : MonoBehaviour
{
   
   

    void Update()
    {
        if (transform.position.x> 20.7)
        {

            Destroy(gameObject);
        }
        else if (transform.position.x > 20.7)
        {
            Destroy(gameObject);
        }
    }
}